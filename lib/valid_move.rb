def valid_move?(board, index)
  if 
    
  else
    false
  end
end

def position_taken?(board, index)
  move = nil
  if (board[index] == " " || board[index] == "" || board[index] == nil)
    move = false
  else
    move = true
  end
end